1. Yes — Wikidata query on apply is correct for architect 
   and engineer filters. For structure type and country 
   filters use Photon/Overpass as already built.

2. I will paste the SQL myself into the Supabase SQL editor. 
   Write the migration SQL, seed script, and RLS policies 
   as separate files I can copy and run. Do not ask for 
   the service role key.

3. Yes start now on everything you can build without 
   those answers.